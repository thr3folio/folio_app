<h1>New Company</h1>


<%= form_tag(companies_url, method: 'post') do %>


  <div>
    <%= label_tag :name %><br />
    <%= text_field_tag :name %>
  </div>

  
  <div>
    <%= submit_tag "Create Company" %>
  </div>
<% end %>
